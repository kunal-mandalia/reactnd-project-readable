version: 2
jobs:
  build:
    working_directory: ~/reactnd-project-readable
    machine:
      node:
        version: 6.1.0
      npm:
        version: 4.2.0
    steps:
      - run:
          name: install
          command: npm install
      - run:
          name: build
          command: npm build
      - run:
          name: test
          command: npm test